# デザイン課題1〜4 テスト計画（docs/35 由来）

> docs/35「デザイン課題と改善方針」の最重要課題1〜4の実装に対する受け入れ・回帰テスト。
> 対象コミット: `feat/sns-infographic-images` ブランチ上の worktree `design-issues-35`。

- 作成: 2026-06-30 / QAエンジニア
- 関連: docs/35（課題）, docs/11（声診断）, docs/01 NFR-05（状態網羅）, docs/31（課金導線）

## 1. スコープ
- 課題1: 声タイプ診断の「未登録でも1回お試し → 結果ぼかし → 登録で開放」
- 課題2: 旧 recordings 画面のブランド統一
- 課題3: 転換導線（FB直後の「次にできること」／レポート forbidden の行き止まり解消）
- 課題4: recordings 系の状態網羅（loading/empty/error/success）＋具体エラー文言

## 2. テスト観点別ケース

| ID | 観点 | 前提 | 手順 | 期待結果 | 関連 |
| --- | --- | --- | --- | --- | --- |
| TC-01 | セキュリティ/境界(自動) | 未ログイン | `POST /api/v1/voice-type/analyze` に非対応拡張子で送信 | **401を返さない**（400 BAD_REQUEST）。＝匿名解析が許可されている | 課題1 |
| TC-02 | 正常系 | 未ログイン | 録音/アップロードで診断完走 | 結果カードが**ぼかし表示**＋「無料登録して結果を見る →」。シェアボタンは出ない | 課題1 |
| TC-03 | 正常系 | ログイン済 | 同上 | フル結果＋シェアボタン＋「レッスンを受ける」 | 課題1 |
| TC-04 | 性能/濫用 | 未ログイン | `rate_limit_max_audio`+1 回連続で analyze | 上限超で **429 RATE_LIMITED**（IP単位で計数） | 課題1 |
| TC-05 | 状態遷移 | ログイン済 | 一覧を開く | 取得中は skeleton（loading） | 課題4 |
| TC-06 | 状態遷移 | 録音0件 | 一覧を開く | 空状態カード＋「最初の録音をアップロード →」CTA | 課題4 |
| TC-07 | 異常系 | API停止 | 一覧を開く→再試行 | 生の "Failed to fetch" を出さず**親切文言**＋「再試行する」で再取得 | 課題4 |
| TC-08 | 境界値 | - | new で 20MB 超のファイルを選択 | 送信前に「音声ファイルが大きすぎます（最大 20MB）」。送信されない | 課題4 |
| TC-09 | 正常系 | ログイン済 | new でアップロード成功 | ✅success 表示 → 履歴へ自動遷移（無言遷移でない） | 課題2,4 |
| TC-10 | 正常系 | 評価済 | 評価詳細を開く | FB直後に「次にできること 🎯」カード → レポートへ遷移 | 課題3 |
| TC-11 | 状態遷移 | 無料/上限 | レポート forbidden を開く | アップグレードと**「評価に戻る」を両方表示**（行き止まりでない） | 課題3 |
| TC-12 | 回帰 | - | 全画面のブランド | `bg-blue-600`/`text-red-600`/英語生文言が残っていない | 課題2 |

## 3. テスト環境
- frontend: `next dev`（worktree配信）, backend: `uvicorn app.main:app`
- 自動: `cd backend && ./.venv/bin/python -m unittest tests.test_voice_type_anonymous -v`

## 4. 合格基準
- TC-01（自動）が PASS。手動 TC-02〜TC-12 が期待結果どおり。
- 既存自動テスト（test_conversational_fb / test_zero_base_fb）が回帰なし。

## 5. 実施結果（2026-06-30）
- TC-01: **自動テストで PASS**（後述 test_voice_type_anonymous）。
- TC-02 / TC-08 / 一覧error(TC-07): dev サーバ実機で目視 PASS（ゲート撤去・最大20MB文言・親切エラー＋再試行）。
- TC-04/TC-09/TC-10/TC-11: バックエンド＋ログイン＋データ要のため手動枠（実機通し検証で確認）。
