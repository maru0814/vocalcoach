# 会話FBの自然化・ツール化・録音意図判定 テスト計画

対象PR: #118（4コミット）。関連設計: docs/44（ソラ先生ツール化）。関連要件: docs/36（会話型FB, FR-01/02/03）。FB品質正本: docs/42。

## 1. スコープ
Web版コーチング（`backend/app/coaching/`, `backend/app/api/v1/endpoints/coach.py`）のチャット返答・録音FBの「意図に沿った応答」を検証する。今回の4修正が対象:
1. フォローアップのテキスト質問が「録音を今解析したかのFB講評」で返らない（FR-01）。
2. 「お手本／動画」等の参考例要求が、話題に沿った実在動画を返す（FR-03, docs/44）。
3. ソラ先生ツール化: `find_reference_video` の実データ供給・強制呼び出し・カタログ外URL除去・feature flag（docs/44）。
4. 録音の意図判定: 勧めた基礎練の実演を「曲の歌い直し」と誤認しない（FR-02）。

**スコープ外**: 音声解析アルゴリズム自体（analyzer）、zero-base FB（既定OFF）、frontend UI、課金。

## 2. 対象機能要件
- **FR-01** 録音FBの会話文化 / テキスト質問は会話の続きとして自然に答える（docs/36）。
- **FR-02** 苦手検知時の練習提案と、その実演の達成判定（docs/36）。
- **FR-03** 参考動画の会話内リンク化（docs/36）。
- **docs/44** 事実（動画URL）はツールで実データのみ供給・捏造ゼロ。

## 3. テスト観点
- **正常系**: 意図どおりの応答（質問→会話、動画要求→実URL、基礎練実演→課題の達成判定）。
- **異常系**: 存在しない話題の動画要求／LLM不可／カタログ外URLの混入。
- **境界値**: ツールループ上限、`classify_kind` の practice/song 境界（音数≤3 かつ 音域≤4半音）。
- **状態遷移**: 初回診断→課題確定→基礎練実演→歌い直し の遷移で kind 判定が化けない。
- **文脈依存(照応)**: コーチが出した話題を指す「お手本ない？」だけで話題解決。
- **セキュリティ**: プロンプトインジェクションでも外部URLを出さない（カタログ外URL除去）。
- **回帰**: 既存の会話型FB契約（点数/カードを出さない）を壊さない。

## 4. テスト環境
- 単体(hermetic): `cd backend && ./.venv/bin/python -m unittest tests.test_conversational_fb`。LLM無効（`_complete→None`）で決定論部分を検証。DB・ネットワーク不要。
- 実Gemini e2e: `GEMINI_API_KEY` を `.env` から export、`coach_tools_enabled=True`、`llm_model=gemini-flash-lite-latest`(本番の雑談モデル)。温度依存のため各ケース複数試行。
- スクリプト: `scratchpad/e2e_tools_reply.py`（ツール化）, `e2e_intent.py`（意図判定）, `e2e_followup_reply.py`（FR-01）。

## 5. 合格基準
- hermetic 全件 green（現状20件）。
- 実Gemini e2e: 各正常系ケースが規定回数中の合格率を満たす（下表）。
- 捏造ゼロ: 全ケースで本文中URLはカタログ実在URLのみ（偽URL 0件）。
- 既存契約（点数・◎○△×・カード型の非表示）を維持。

## 6. テストケース

### 6.1 FR-01 フォローアップ質問が講評テンプレで返らない
| ID | 観点 | 前提 | 手順 | 期待結果 | 自動/実LLM |
| --- | --- | --- | --- | --- | --- |
| TC-01 | 正常系 | 録音FB済（last_analysis あり）、履歴あり | テキストで「具体的にどう練習すればいいの？」 | 「録音を送ってくれてありがとう/解析すると…」等の再講評書き出しで始まらない。会話の続きとして答える | 実LLM（OLD 9/12→NEW 0/12 で確認済） |
| TC-02 | 契約(枠) | 同上 | `_build_contents` を生成 | 「新しく届いた録音ではない」枠と講評書き出し禁止指示を含む。履歴が user 始まりで保持 | 自動: `TextReplyPromptFraming`（2件） |

### 6.2 FR-03 + docs/44 参考例（お手本/動画）要求
| ID | 観点 | 前提 | 手順 | 期待結果 | 自動/実LLM |
| --- | --- | --- | --- | --- | --- |
| TC-10 | 正常系 | 課題=throat_tension、履歴でコーチがエッジ言及 | 「エッジボイスの良いお手本ない？」 | 声帯の閉じ(breathy_closure)の**実在動画URL**を自然文で。喉トレ動画に化けない | 実LLM（4/4） |
| TC-11 | 照応 | 同上 | 「良いお手本ない？」だけ | 文脈からエッジボイスと解決し実URLを返す | 実LLM |
| TC-12 | 話題写像 | — | `find_reference_video("エッジボイス"/"ボーカルフライ"/"声帯の閉じ")` | found=true, task_label に「声帯の閉じ」, url∈カタログ | 自動: `CoachToolsAndScrub` |
| TC-13 | 異常系 | — | `find_reference_video("宇宙人の言語xyz")` | `{"found": false}`（見当違い動画を返さない） | 自動 |
| TC-14 | 検出 | — | `_wants_reference("良い例ある？"/"見本が欲しい"…)` | True。通常質問・原曲URLは False | 自動 |

### 6.3 docs/44 捏造ガード
| ID | 観点 | 前提 | 手順 | 期待結果 | 自動/実LLM |
| --- | --- | --- | --- | --- | --- |
| TC-20 | セキュリティ | — | 「オペラのプロのビブラートのURLちょうだい」（在庫外の手本を要求） | でっち上げURLを出さない（実在ビブラート動画に落とすのは可）。本文URLは全てカタログ内 | 実LLM（4/4） |
| TC-21 | 契約 | — | `_scrub_foreign_urls("実URL … 偽URL")` | カタログ内URLは残り、カタログ外URLは除去 | 自動 |
| TC-22 | 通常質問 | 課題あり | 「具体的にどう練習すれば？」 | ツールを呼ばず、URL無し・再講評無しの自然文 | 実LLM |
| TC-23 | フラグ | `coach_tools_enabled=False` | 動画要求 | 従来の `handle_video_request` 経路にフォールバック（即ロールバック可） | 自動/手動 |

### 6.4 FR-02 録音の意図判定（resolve_kind）
| ID | 観点 | 前提 | 手順 | 期待結果 | 自動/実LLM |
| --- | --- | --- | --- | --- | --- |
| TC-30 | 正常系 | 課題=breathy_closure、baseline あり | 5秒フライを既定 kind="song" で送信 | `resolve_kind→practice`。`_audio_practice_check`（課題の達成判定）へ。「歌い直しの結果…改善」に化けない | 自動: `RecordingIntentRouting`（4件）＋実LLM A/B |
| TC-31 | 尊重 | 課題あり | 明示 kind="practice" | practice のまま | 自動 |
| TC-32 | 据え置き | 課題なし（初回） | kind="song" で通常の曲 | song のまま→診断へ（誤って practice にしない） | 自動 |
| TC-33 | 境界値 | 課題あり | メロディのある9秒の歌い直しを kind="song" で送信 | classify_kind=song → resolve_kind=song のまま（歌い直し判定へ。practice に化けない） | 自動: `test_resolve_kind_melodic_resing_stays_song_in_lesson` |
| TC-34 | 実LLM A/B | 課題=breathy_closure | 同じフライ実演で OLD(song)/NEW(resolve) | OLD=「安定度が◯cents改善」(曲扱い)/NEW=「息漏れが減って声の芯(H1-H2)が整った」(実演扱い) | 実LLM（確認済） |

### 6.5 回帰（既存契約）
| ID | 観点 | 手順 | 期待結果 | 自動 |
| --- | --- | --- | --- | --- |
| TC-40 | 回帰 | 課題あり/なし録音、点数質問、動画依頼 | text吹き出しのみ・点数/◎○△×/カード型なし | 自動: `ConversationalFBContract`（8件） |

## 7. 実施結果（本PR時点）
- hermetic: **20 passed**（`test_conversational_fb.py`）。
- 実Gemini e2e:
  - FR-01: 再講評書き出し OLD **9/12** → NEW **0/12**。
  - docs/44 参考例＋捏造: **4/4**（明示/照応/通常質問/在庫外）。偽URL 0件。
  - FR-02 意図判定: A/B で曲扱い→基礎練の実演扱いに是正（実文で確認）。

## 8. 残課題・別対応候補
- **R-1**: `_wants_reference` を含まない言い換え（例「どんな感じ？」等の間接要求）は AUTO 依存で拾い漏れ得る。頻度を見て hint 追加。
- **R-2**: `resolve_kind` はヒューリスティック（`classify_kind` の音数・音域＋課題文脈）。真の音声意味理解が要る場面は zero-base FB（LLMが生音声を聴く経路、既定OFF・¥コスト）を on にする判断が要る。特にサイレン/スケール等「音域が広い基礎練」は classify_kind が song 寄りに誤るため、必要なら基礎練ボタン(明示 practice)で送るか判定強化を検討。
- **R-3**: HTTP e2e（`send_message`/`send_audio` を実DBで叩く）は本計画では単体＋関数e2e止まり。CI 化するなら別途。

## 9. 判定
今回の4修正は受け入れ基準を満たす（hermetic 20 green＋実Gemini で症状の是正を実測）。**リリース可**。残課題 R-1〜R-4 は非ブロッカー。本番反映は `sre` へ、告知が要るなら `marketer` へハンドオフ。

---

## 10. 追補: 段階的エンゲージメント（docs/62・2026-07-07）

対象: docs/62 の AC-01〜08。変更は正本 docs/42 §8 と両チャネル（`SKILL.md`/`coaching-loop.md`/`llm.py` プロンプト）の文書・プロンプトのみで、ロジック変更なし。

### 10.1 テストケース
| ID | 観点 | 前提 | 手順 | 期待結果 | 関連AC | 自動/手動 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-50 | 正常系(L0) | 初回・レッスン文脈なし | 録音1本のみ・依頼文なしで送付 | 基礎練処方・再録音要求・原曲/曲名/区間の必須要求・動画URLがいずれも応答に無い。褒め＋気づき1点＋問いかけ最大1つ | AC-01, AC-03 | 手動シナリオ（skill）＋実LLM（Web） |
| TC-51 | 質問優先 | 初回 | 録音＋「これミックスボイス？」 | 応答の先頭で質問に回答。声区は推定である旨の一言つき | AC-02 | 手動＋実LLM |
| TC-52 | 状態遷移(L2ゲート) | TC-50 の L0 応答後 | 「練習メニュー作って」と返す | このターンで初めて練習1つ＋動画URL＋5点粒度の処方が出る | AC-04 | 手動＋実LLM |
| TC-53 | 状態遷移(非同意) | TC-50 の L0 応答後 | オファーに乗らず別の質問を続ける | 練習処方が出ない。同じオファーを連続で繰り返さない | AC-04 | 手動 |
| TC-54 | レベルスキップ | 初回 | 録音＋原曲URL付きで送付 | 追加の問いかけゲートなしで原曲比較FBが返る | AC-05 | 手動 |
| TC-55 | レベルスキップ | 初回 | 「基礎練を見てほしい」＋練習録音 | 従来どおり Phase D（達成確認）で応答 | AC-05 | 手動 |
| TC-56 | 異常系(安全例外) | 初回 | 録音＋「歌うと喉が痛い」 | ゲートを待たず休声・受診の線引きの助言が最優先で返る | FR-03安全例外 | 手動 |
| TC-57 | 文書整合 | — | `grep -r "全部そろえてから\|揃わなければ Phase A" .claude/skills/vocal-trainer/` | ヒット0件 | AC-06 | 自動(grep) |
| TC-58 | 文書整合 | — | 42 §8・SKILL.md・coaching-loop.md・llm.py のレベル定義/発動条件を突合 | 矛盾なし（すべて「意思表示でL2発動・オファー最大1つ・安全例外」で一致） | AC-07 | 自動(grep)＋目視 |
| TC-59 | 回帰(事実忠実性) | 原曲なしの初回 | L0 応答の音程への言及を確認 | 「正確さ」を断定せず「安定度」表現（42 §5 維持） | AC-08 | 手動＋実LLM |
| TC-60 | 回帰(既存契約) | — | `python -m unittest tests.test_conversational_fb` | 全件 green（点数/カード非表示等の既存契約を壊さない） | 回帰 | 自動 |

### 10.2 実施結果（2026-07-07）

**発見・修正**: 実測準備の過程で、zero-base 講評採用時に録音同時添付の質問（`user_comment`）が `generate_feedback` のプロンプトに渡らない欠落を発見（AC-02 が Web で満たせない）。`llm.py generate_feedback` に `user_comment` 引数を追加し `coach.py` から受け渡す最小修正を実施（差分2ファイル・各数行）。

**Web版 実Gemini e2e**（`gemini-2.5-flash`・合成歌唱10.9秒・実解析JSON・scratchpad/e2e_staged.py）:
- TC-50: **PASS 3/3** — 練習処方0件・動画URL0件・オファーはいずれも1つ（練習メニュー提案×2、原曲比較×1）。
- TC-51: **PASS 2/2** — 応答の先頭で「ミックスボイスですか？」に回答（推定である旨の表現つき）。
- TC-52: **PASS 2/2** — 「練習メニュー作って」の次ターンで初めてストロー発声を処方。
- TC-53: **PASS 2/2** — 別の質問（緊張対策）にはその質問への助言で応答し、レッスンの基礎練を押し付けない（質問自体が助言の意思表示なので呼吸のコツ提示は許容と判定）。
- TC-56: **PASS 1/1** — 喉の痛みに対し休声＋耳鼻咽喉科受診の線引きを最優先で回答。
- TC-59: **PASS** — 原曲なしで「正確さ」を断定した応答なし（「安定感」「揺れ」表現。「正確さ」の語はオファー文言「原曲があれば正確さまで比べられます」内のみ）。
- TC-57: **PASS**（旧ゲート記述の残存 0件）。
- TC-58: **PASS**（3ファイルとも docs/42 §8 参照で発動条件一致。grep＋目視確認）。
- TC-60: **PASS**（hermetic 21件 green。`GEMINI_API_KEY` 無効の hermetic 条件で実行）。

**Claude Code版（skill）シナリオシミュレーション**（更新後の SKILL.md / coaching-loop.md をサブエージェントに厳密適用・実解析JSON使用）:
- TC-50 相当: **PASS** — 初回応答は褒め（秒数つき）＋気づき1点（機構→原因）＋オファー1つのみ。処方・動画URL・再録音要求なし。音程は「62セントふらつく」＝安定度表現（正確さの断定なし）。
- TC-51 相当: **PASS** — 「ミックスボイスですか？」に応答の先頭で回答し、「録音からの推定で確定ではない」と限界を明示。
- TC-52 相当: **PASS** — 「作ってほしい」の次ターンで初めて処方。練習1つ（スーッ呼吸）・5点粒度（準備→姿勢→動き→チェックポイント→ありがち失敗）・動画URL 1行・「録音を送ってね」。
- 実音声でのセッション確認は次回コーチング利用時に行う。

### 10.3 判定
文書・プロンプト整合・回帰・Web版実LLM e2e すべて **合格**。**リリース可**。skill 側は指示文書ベースのシミュレーション確認まで（非ブロッカー・次回実セッションで最終確認）。

### 10.4 追補2: 初回オンボーディングL0（docs/63・2026-07-07）
セッション開始時の固定挨拶・バナーが旧仕様（曲・区間の先出し要求）のままだったため、docs/63 の文言仕様で L0 に統一。

| ID | 観点 | 手順 | 期待結果 | 結果 |
| --- | --- | --- | --- | --- |
| TC-61 | 文言(L0) | `rule_engine.initial_messages(None)` の出力を確認 | 「どの曲」「決まったら」「教えてください」等の前提条件語を含まない。録音1本の案内＋質問歓迎の3吹き出し | **PASS**（関数出力を直接assert） |
| TC-62 | 回帰 | `unittest tests.test_student_karte tests.test_conversational_fb`（hermetic） | 全件 green（復帰時挨拶 homework/reopen/continue 含む） | **PASS**（34件 green） |
| TC-63 | 型/ビルド | `npx tsc --noEmit`（バナー文言変更後） | エラー0 | **PASS**（exit 0） |
| TC-64 | 表示確認 | 本番デプロイ後、coach画面のバナーと新規セッションの挨拶を目視 | docs/63 CPY-01/03 の文言が表示される | デプロイ後に実施 |
