# メール文面 ウェルカム便（正本）

- 文書番号: 86
- 作成日: 2026-08-01
- 作成: マーケター
- 関連: docs/84（FR-04）, docs/85（実装先: `backend/app/services/mail_service.py`）, docs/42（ソラ先生トーンの正本）
- **運用ルール**: 文面を変えるときはまずこの86番を更新し、`mail_service.py` の定数へ反映する（docs/42 と同じ単一ソース規律）。

## 設計方針
- 差出人はソラ先生名義。トーンは docs/42 §1（わたし・やわらかい敬体・絵文字0〜2個）。
- CTAは1つだけ: 初回録音（`{frontend_base_url}/coach`）。声診断・プレミアムには触れない。
- 抽象的な比喩は使わない。何ができるかをストレートに書く。
- **HTML＋プレーンテキストのマルチパート**（2026-08-01 HTML版追加。§HTML版デザイン仕様）。テキスト版は本文300字以内で全クライアントのフォールバックを兼ねる。
- フッターに運営者連絡先を明記（特定電子メール法配慮。docs/84 FR-04）。

## 件名（採用＋予備2案）
| # | 件名 | 位置づけ |
| --- | --- | --- |
| 1 | **ご登録ありがとうございます。最初の1曲、聴かせてください** | **採用**: 用件＋次の行動が件名だけで伝わる |
| 2 | ソラです。あなたの声、まだ聴けていません | 予備: 開封率重視。ただし初回接触では馴れ馴れしさのリスク |
| 3 | 登録完了のお知らせと、はじめの一歩 | 予備: 最も事務的で安全 |

## Who / What / How（2026-08-01 改訂・運用者FBによる再定義）
初見の受信者が件名→ヘッダー→本文の順に読んで、この3つが即答できること:
- **Who（誰から）**: AIボーカルトレーナー「ソラ先生」。ヘッダーは対外正式名 **「AIボーカルトレーナー ソラ先生」＋タグライン「歌をAIが解析・添削」**（`frontend/src/app/layout.tsx` の SITE_NAME/TAGLINE と一致させる）。内輪のブランド名「ソラのステージ」単体は使わない。
- **What（何のサービス）**: 歌の録音を送ると**「直す場所」と「直し方」**が返ってくる（LP docs/17 のソリューション文と同軸）。
- **How（どう使う）**: ①スマホで30秒録音 → ②チャットに送る → ③その場でFB、の**3ステップを本文に明示**する。
- **CTA**: **「無料で歌を見てもらう」**。読み手主語・無料明示・**LPのCTAと同一文言**（メールとLPで約束をずらさない）。「録音を聴かせてください」（ソラ主語のお願い形）は廃止。

## 本文（採用版）

変数: `{cta_url}` = `settings.frontend_base_url + "/coach"`、`{contact_email}` = `settings.mail_from_address`

```
こんにちは、AIボーカルトレーナーのソラです。
ご登録ありがとうございます🎵

わたしは、あなたの歌の録音から「直す場所」と「直し方」を
具体的にお返しするAIボーカルトレーナーです。

使い方は3ステップです。
1. スマホでワンフレーズ録音する（30秒でOK）
2. チャットにアップロードする
3. その場でフィードバックが届く

▼ 無料で歌を見てもらう
{cta_url}

それでは、お待ちしていますね。

--
ソラ先生（Vocal Coach）
お問い合わせ: {contact_email}
このメールはご登録手続きの完了をお知らせする通知です。
```

- 文字数: 約240字（フッター除く本文）。
- 「30秒でOK」= 初回投稿のハードルを下げる一文（登録→初回録音の離脱対策。docs/84 §1 の課題に対応）。

## HTML版デザイン仕様（2026-08-01 追加・デザイナー）

### メールクライアント制約（実装の前提）
- レイアウトは**テーブル**、装飾は**全てインラインCSS**（`<style>`ブロック・外部CSSはGmailで不安定）。
- 画像は **https絶対URLのみ**。Gmailは data URI を表示しない。ロゴは本番配信済みの `{frontend_base_url}/icons/icon-192.png`（51KB・PWAアイコン流用）を使う。
- Web フォント不可。`sans-serif` システムフォントスタック。
- HTMLを送る時も**テキスト版を必ず同梱**（マルチパート。表示不可クライアント・スパムスコア対策）。

### 構造（1カラム・最大600px）
```
┌─ ページ背景 #f4f5f7 ────────────────────┐
│ ┌─ カード 600px・白・角丸12px ─────────┐ │
│ │ ヘッダー: 紺 #171045                  │ │
│ │   ソラのアイコン（64px・円形）        │ │
│ │   「AIボーカルトレーナー ソラ先生」   │ │
│ │     白・太字16px（Who＝対外正式名）   │ │
│ │   「歌をAIが解析・添削」#c7d2fe・12px │ │
│ │ 本文: 白背景・#1f2937・15px/1.9       │ │
│ │   挨拶＋What（直す場所と直し方）      │ │
│ │   How: 3ステップ（シアン数字①②③）    │ │
│ │   （テキスト版と同一コピー）          │ │
│ │ CTAボタン（中央・1つだけ）:           │ │
│ │   「無料で歌を見てもらう」            │ │
│ │   bg #22d3ee／文字 #0f0a2e 太字        │ │
│ │   padding 14px 36px・角丸8px          │ │
│ │ 結び: 「お待ちしていますね。」        │ │
│ │ フッター: #6b7280・12px               │ │
│ │   差出人名義／お問い合わせ／通知文言  │ │
│ └───────────────────────────────────────┘ │
└───────────────────────────────────────────┘
```
- 冒頭に**非表示プリヘッダー**「登録ありがとうございます。録音1本で、次に練習することが分かります。」（受信一覧のプレビュー行を制御）。
- CTAはテーブル製ボタン（bulletproof button）。`<a>`のみのボタンはOutlookで崩れるため。
- ダークモード反転には抗わない（メディアクエリ非対応クライアントが多いため、単色・高コントラストで成立させる）。

### アクセシビリティ / コントラスト
- ロゴ `alt="ソラ先生"`。画像ブロック時も本文だけで意味が通る構成。
- ボタン: #0f0a2e on #22d3ee ≒ 12:1、ヘッダー: 白 on #171045 ≒ 15:1（いずれもWCAG AA以上）。
- リンクはボタンに加えフッターに素のURLも1行併記（コピー&ペースト用・リンク先の透明性）。

### 差出人アイコン（Gmailで丸く出るアバター）について
メールHTMLでは制御できない。出す方法は2つで、現構成で可能なのは(1)のみ:
1. **benfan164@gmail.com のGoogleアカウントのプロフィール写真を設定**（myaccount.google.com → 個人情報 → 写真 → `frontend/public/brand/sora-icon.png` をアップロード）。Gmail宛の受信者にはこれが表示される。反映まで数時間〜48時間かかることがある。
2. BIMI（全クライアント共通の公式手段）→ 独自ドメイン＋DMARC必須のため現構成では不可。独自ドメイン移行時に再検討。

## 実送信の確認手順（運用者向け・キー設定後に1回）
1. Brevo でアカウント作成 → Senders で送信者アドレスを認証 → API キー発行（`xkeysib-...`）。
2. VPS `/opt/vocalcoach/docker/.env` に追記:
   `MAIL_API_KEY=xkeysib-...` / `MAIL_FROM_ADDRESS=<認証済みアドレス>`
3. backend を再起動（docs/85 §11）。
4. 自分の別アドレスで新規登録 → ウェルカムメールが届くこと・CTAリンクが本番 `/coach` を開くことを確認。
5. 届かない場合: `docker compose logs backend | grep "welcome mail"` で sent/skip/failed を確認。
