fastapi==0.115.6
uvicorn[standard]==0.30.6
pydantic-settings==2.7.1
sqlalchemy==2.0.36
pymysql==1.1.1
alembic==1.14.0
python-multipart==0.0.12
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
email-validator==2.2.0

# 自然言語チャット応答（ソラ先生の対話 / Google Gemini。未設定時はルールベースにフォールバック）
google-genai==2.7.0

# チャット経路の他社バックエンド検証（docs/103 B案）。OPENAI_API_KEY 未設定なら未使用。
# 音声・分析経路は Gemini のままで、これはチャットのみに使う。
openai==2.54.0

# audio analysis pipeline (tools/audio_analyzer.py)
librosa==0.11.0
numpy==2.4.6
scipy==1.17.1
soundfile==0.13.1
yt-dlp==2026.3.17
imageio-ffmpeg==0.6.0
praat-parselmouth==0.4.7
stripe==15.2.0

# Web Push（練習リマインド通知 / PWA。docs/73〜75。VAPIDで標準Web Push Protocolを送信）
pywebpush==2.0.0

# ウェルカムメール送信（Brevo REST API。docs/84/85。pywebpush 経由の推移依存を明示ピン）
requests==2.34.2
