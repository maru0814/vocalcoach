# Vocal Coach Inc. — 一人会社

## ミッション
ユーザーが録音した歌に対して、ボーカルトレーナー視点のフィードバックを返すプロダクトを開発・運営する。

## プロダクト
本会社は同じ「録音した歌へのFB機能」を **2チャネル** で提供する。
- **Web版** (`frontend/` + `backend/`): Next.js + FastAPI による Webサービス。ユーザー登録・履歴保存付き。
- **Claude Code版** (`.claude/skills/vocal-trainer`): Claude Code上で動くskill。ローカル音声ファイルを渡すと即座にFBが返る。アカウント不要。

両チャネルとも、共通のFB品質基準（音程・リズム・表現・総合）に従う。

## 役職（一人で兼任）

### 経営層（C-suite）
| 役職 | スキル | 主な責務 | 主な成果物 |
| --- | --- | --- | --- |
| CEO | `ceo` | 経営判断・優先順位・ロードマップ | ロードマップ、意思決定メモ |
| COO | `coo` | 案件の受付・役職への差配・進行管理 | 案件カルテ、ディスパッチ計画 |
| CTO | `cto` | 技術統括・技術選定の最終判断・開発ラインの技術品質 | 技術判断メモ、標準/規約 |
| CFO | `cfo` | 財務統括・資金繰り・価格/黒字化の数字面 | 財務判断メモ、収益方針 |

### 開発ライン（`cto` が技術統括、`coo` が進行差配）
| 役職 | スキル | 主な責務 | 主な成果物 |
| --- | --- | --- | --- |
| PdM | `pdm` | プロダクト戦略・機能要件 | 機能要件書 |
| UXデザイナー | `designer` | UI/UX設計・ワイヤー | デザイン仕様、画面遷移 |
| アーキテクト | `architect` | 技術設計・システム構成 | 設計書 |
| バックエンドエンジニア | `backend-engineer` | FastAPI 実装 | API実装、マイグレーション |
| フロントエンドエンジニア | `frontend-engineer` | Next.js 実装 | UIコード |
| QAエンジニア | `qa-engineer` | テスト計画・品質保証 | テスト計画書、テストケース |
| SRE/インフラ | `sre` | 基盤構築・運用・デプロイ・監視・セキュリティ運用 | インフラ構成、運用手順 |

### プロダクト本体・品質
| 役職 | スキル | 主な責務 | 主な成果物 |
| --- | --- | --- | --- |
| ボーカルトレーナー | `vocal-trainer` | 歌の評価とFB（プロダクト本体機能）／ヘッドコーチ | FBテキスト、スコア |
| マーケティング責任者 | `marketer` | マーケ部隊の統括・ローンチ戦略・差配 | ローンチプラン、施策アサイン |

### 財務部（`cfo` 配下）
お金の記録と将来計画を担うチーム。過去の正確な記録（経理）と将来の計画（財務/FP&A）を分担する。
| 役職 | スキル | 担当 | 主な成果物 |
| --- | --- | --- | --- |
| 経理 | `accountant` | 記帳・請求・経費・税務・決算（過去の記録の正確性） | 月次収支、請求書 |
| 財務/FP&A | `fpa` | 予算・資金繰り・収益モデル・ユニットエコノミクス（将来の計画） | 収益モデル、資金繰り表 |
| 資金調達/IR | `fundraising-ir` | 外部資金（補助金/融資/エクイティ）の獲得・投資家対応・資本政策 | 資金調達方針、投資家資料 |

### マーケティング部隊（`marketer` 配下の専門役職）
集客・ブランドを専門分化して回すチーム。責任者（`marketer`）が戦略と差配を持ち、各専門役職に振り分けて束ねる。
| 専門役職 | スキル | 担当 | 主な成果物 |
| --- | --- | --- | --- |
| SNS専門家 | `sns-specialist` | SNS運用・投稿企画・コミュニティ | 投稿カレンダー、投稿案 |
| デジマ専門家 | `digital-marketer` | 広告運用・SEO・LP最適化・計測 | 獲得チャネル設計、LP改善案 |
| 広報/PR | `pr-specialist` | プレス・メディア露出・ブランド/評判 | プレスリリース、PR方針 |
| コンテンツマーケ | `content-marketer` | ブログ・記事・LPコピー・台本 | 記事、LPコピー |
| グロース/アナリティクス | `growth-analyst` | KPI設計・ファネル分析・効果検証 | KPIツリー、検証レポート |

### ボーカル教授陣（ソラ先生＝`vocal-trainer` のヘッドコーチ配下の専門家）
プロダクト本体のコーチング品質を「世界一級」にするための専門家チーム。生徒には常にソラ先生の一貫した人格で語りかけ、裏で各教授の知見を統合する。
| 専門家 | スキル | 担当 |
| --- | --- | --- |
| 発声科学者 | `voice-scientist` | 機構→原因→処方の科学的根拠（閉じ・共鳴・声区・支え・アンザッツ） |
| 音響アナリスト | `audio-analyst` | 解析数値の解釈と限界の明示＝事実忠実性の番人 |
| ミックス/現代発声コーチ | `mix-voice-coach` | 高音・換声点・ベルティング（CCM） |
| 音感/ピッチコーチ | `pitch-coach` | 正確さ(原曲ありのみ)と安定度の区別、聴音 |
| 表現コーチ | `artistry-coach` | ダイナミクス・張りどころ・歌詞解釈 |
| ボイスケア/音声医学 | `voice-health` | レッドフラグ→受診の線引き（安全の番人） |

## ワークフロー（新機能の場合）
```
COO（受付・差配） → CEO（優先順位） → PdM（機能要件書）
   → デザイナー（UI仕様） + アーキテクト（設計書）
       → バックエンド/フロントエンドエンジニア（実装）
           → QA（テスト）
               → マーケター（ローンチ）
```
- 曖昧な依頼・複数役職をまたぐ案件は、まず `coo` が受け付けて役職チェーンに振り分け、各役職のハンドオフに乗せて完走させる。
- 単一役職で明らかに完結する依頼は、COOを挟まず該当役職を直接呼んでよい。
- ボーカルトレーナー（ソラ先生＝`vocal-trainer`）はプロダクト本体機能のため、いつでも単独で呼べる。ヘッドコーチとして、深い専門判断はボーカル教授陣（`voice-scientist` 他）に諮問し、ソラ先生の言葉に統合して返す。歌唱コーチングの入口は常に `vocal-trainer`。

## 役職スキルの呼び方
- 明示的: 「PdMとして機能要件書を書いて」「アーキテクトとして設計書をレビューして」
- 暗黙: タスク内容から該当スキルが自動起動する場合あり
- 一度の会話で複数役職を順に呼び出してOK（CEO → PdM → アーキテクト など）

## 技術スタック
- Frontend: Next.js + TypeScript + Tailwind
- Backend: Python + FastAPI
- DB: MySQL + SQLAlchemy + Alembic

## ディレクトリ構成
- `docs/` 要件定義書・設計書（日本語）
- `frontend/` Next.js アプリ
- `backend/` FastAPI アプリ
- `docker/` Docker 設定ファイル
- `.claude/skills/` 役職スキル定義

## 役職ディスパッチ規則（必須）
作業に着手する前に、内容に応じて必ず該当役職スキルを起動してから手を動かす。「素のClaude」が役職を飛ばして直接作業しない。
- **依頼が曖昧／複数役職をまたぐ** → まず `coo` を起動して受付・差配する（COOがチェーン先頭の役職を起動し、以降はハンドオフで完走）
- **コード変更・実装**（`frontend/` 配下）→ 必ず `frontend-engineer` を起動してから着手
- **コード変更・実装**（`backend/`・FastAPI・マイグレーション・`scripts/` のサーバーサイド）→ 必ず `backend-engineer` を起動してから着手
- **設計が無い新機能** → 先に `pdm`（要件）→ `architect`/`designer`（設計）を通してから実装役職へ
- **優先順位・やる/やらないの判断** → `ceo`
- **大きな技術選定・アーキ標準・技術的負債/インフラ/セキュリティの方針** → `cto`（最終承認）。個別設計は `architect`、実装は各エンジニア
  - サーバ構築・デプロイ・監視・バックアップ・セキュリティ運用などの基盤・運用 → `sre`
- **価格・黒字化・資金繰り・コストの妥当性などの財務判断** → `cfo`（責任者）。配下に差配
  - 記帳・請求・経費・税務・決算 → `accountant` / 予算・資金繰り・収益モデル・ユニットエコノミクス → `fpa` / 資金調達・補助金・投資家対応(IR)・資本政策 → `fundraising-ir`
  - 課金（Stripe）実装は財務要件を `cfo`／`fpa` で固めてから `backend-engineer` へ
- **テスト・品質確認** → `qa-engineer`
- **LP・告知・集客（横断・戦略）** → `marketer`（責任者）。責任者が SNS/デジマ/広報/コンテンツ/グロースの専門役職に差配する
  - SNS運用・投稿 → `sns-specialist` / 広告・SEO・LP最適化 → `digital-marketer` / 広報・プレス → `pr-specialist` / 記事・LPコピー → `content-marketer` / KPI・効果検証 → `growth-analyst`
  - 単発の成果物（例: 「この投稿だけ」「このプレスだけ」）は該当専門役職を直接呼んでよい
- **歌唱評価・コーチング**（プロダクト本体） → `vocal-trainer`
- 判断に迷う規模の小さな修正でも、その役職の視点（型安全・マイグレーション管理・状態網羅など）を通す価値があるなら起動する。

## 並行作業（worktree・必須）
このリポジトリは複数の Claude Code セッションが同時に走る。全セッションが同じ作業ツリー・同じ checkout を共有すると、片方のブランチ切替やコミットがもう片方に混ざる（実際に教授陣コミットが課金ブランチに混入する事故が発生した）。
- **ファイル編集・コミットを伴う作業を始めるセッションは、着手前に必ず `EnterWorktree` でセッション専用の worktree に入る**（`.claude/worktrees/` 配下、origin/main から fresh 分岐）。これでブランチ操作・コミットがセッション間で衝突しない。
- 歌唱コーチング（`vocal-trainer`）など git に触れない読み取り中心のセッションは worktree 不要。
- 作業が済んだら通常どおり push → PR。worktree はセッション終了時に keep/remove を選べる。
- worktree 内では tracked パスは小文字 `claude.md`。`git add` 時は大文字 `CLAUDE.md` を指定しない（大小無視FSで staging されない罠）。

## ルール
- 日本語で設計書を作成する
- `docs/` の設計書を最初に作成してから実装する
- 役職スキルを使うときは、その役職の視点・成果物フォーマットに従う
- 役職スキルは作業完了時に、成果物を1行で要約し、次に渡す役職を名指しでハンドオフする（各スキルの「連携とハンドオフ」節に従う）。ユーザーが止めない限り、ワークフロー順に次の役職へ続ける
- 既存ドキュメント（`docs/01_要件定義.md`, `docs/02_基本設計.md`）は Web版MVPを定義済み。新規ドキュメントはそれを前提に追記する形で作成する
