---
paths: ["docs/**"]
---

# docs/ の規約

- ファイル名は `<連番>_<種別>_<内容>.md`（例: `66_設計書_会話モードと対話モデル格上げ.md`）
- 連番は **origin/main の最新を見てから**採る。ローカルの main は古いことがある
  （過去に番号が衝突して付け直しが発生した）
- 種別は 設計書 / デザイン仕様 / 本番運用メモ / ローンチ告知 などの既存語を使う。新語を増やさない
- 既存ファイルの番号は変えない。差し替えるときは新しい番号で追加し、旧番号に「→ NN に移動」と1行残す
